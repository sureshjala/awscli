pipeline {
    agent { label py39 }
    stages {
        stage (vcs) {
            steps {
                git branch: 'master', url: 'https://github.com/sureshjala/awscli.git'
            }
        }
        stage (excute) {
            steps {
                sh './awscli.sh'
            }
        }
    }
}
