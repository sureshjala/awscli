pipeline {
    agent { label 'py3.9' }
    stages {
        stage (vcs) {
            steps {
                git branch: 'master', url: 'https://github.com/sureshjala/awscli.git'
            }
        }
        stage (excute) {
            steps {
                sh './awscli.sh'
            }
        }
    }
}
