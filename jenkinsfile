pipeline {
    stages {
        stage (vcs) {
            steps {
                git branch: 'main', url: 'https://github.com/sureshjala/awscli.git'
            }
        }
        stage (excute) {
            steps {
                sh './awscli.sh'
            }
        }
    }
}
